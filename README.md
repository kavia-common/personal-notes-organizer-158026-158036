# personal-notes-organizer-158026-158036

This workspace contains:
- notes_backend (Django API) – provides REST endpoints
- notes_frontend (Nuxt 3) – user interface

Important:
- Configure the frontend API base via environment variable NUXT_PUBLIC_API_BASE_URL (see notes_frontend/.env.example).
- Start backend first, then run the frontend dev server.